development:
  sessions:
    default:
      database: mongosample
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      uri: <% ENV['MONGOHQ_URL'] || ENV['MONGOLAB_URI'] %>
